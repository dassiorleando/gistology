/**
 * Environment used when building the frontend with 'ng build --prod'
 * @author dassiorleando
 */
export const environment = {
  production: true,

  // You may be using a different API link for prod
  serverUrl: 'https://my-online-server.com'
};
